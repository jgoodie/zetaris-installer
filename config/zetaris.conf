# Zetaris Configuration
export ZETARIS_VERSION="v2.4.1.0-latest"
export ZETARIS_TOKEN="<token_provided_by_zetaris>"
export ZETARIS_RELEASE="helm_latest"

# Environment Settings
export ENVIRONMENT="local"
export STORAGE_CLASS="nfs-rwx"
export STORAGE_CLASS_CREATE="false"
export DNS_PROTOCOL="http"
export DNS_DOMAIN="home.local"
export BASE_DNS_NAME="test.home.local"
export ZETARIS_NS="zetaris"
export PRIVATE_AI_GPU="false"
export DEPLOYMENT_NAME="zetaris-lightning"

# Database Configuration
export DB_HOST="postgres"
export DB_PORT="5432"
export DB_USER="rds_admin"
export DB_PASSWORD="Zetaris123"
export DB_SSLMODE="disable"
export META_DB=metastore
export AUDIT_DB=auditlog
export AIRFLOW_DB=airflowdb

# Auto-generated (don't edit manually)
export METASTORE_JDBC_URL="jdbc:postgresql://${DB_HOST}:${DB_PORT}/${META_DB}?user=${DB_USER}&password=${DB_PASSWORD}&sslmode=${DB_SSLMODE}"
export AUDITLOG_JDBC_URL="jdbc:postgresql://${DB_HOST}:${DB_PORT}/${AUDIT_DB}?user=${DB_USER}&password=${DB_PASSWORD}&sslmode=${DB_SSLMODE}"

export ZETARIS_SERVER_IMAGE=zetregistry.azurecr.io/lightning-server:$ZETARIS_VERSION
export ZETARIS_API_IMAGE=zetregistry.azurecr.io/lightning-api:$ZETARIS_VERSION
export ZETARIS_GUI_IMAGE=zetregistry.azurecr.io/lightning-gui:$ZETARIS_VERSION
export ZETARIS_COMPUTE_SPARK_IMAGE=zetregistry.azurecr.io/lightning-server:$ZETARIS_VERSION
export ZETARIS_COMPUTE_PRESTO_IMAGE_REPO=zetregistry.azurecr.io/lightning-presto-app
export ZETARIS_COMPUTE_PRESTO_IMAGE_TAG=z$ZETARIS_VERSION
export ZETARIS_ZEPPELIN_IMAGE=zetregistry.azurecr.io/lightning-zeppelin-app:z$ZETARIS_VERSION
export ZETARIS_ZEPPELIN_PV_SIZE="2Gi"

### Change this to point to your key directory ###
KEY_DIR="/home/john/zetaris/keys"
export ZETARIS_PRIVATE_KEY_DER=$(cat ${KEY_DIR}/private_key_zetaris.der.b64)
export ZETARIS_PUBLIC_KEY_DER=$(cat ${KEY_DIR}/public_key_zetaris.der.b64)

# Initial Lightning user account
# Usage: ./dev-account.sh email password orgname
export LIGHTNING_INIT_EMAIL="zetaris@dev.local"
export LIGHTNING_INIT_PASSWORD="Zetaris123!"
export LIGHTNING_INIT_ORG="zetaris"


